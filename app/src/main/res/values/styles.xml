<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="mybutton" parent="Widget.AppCompat.Button">
        <item name="android:background">@color/theme_color</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:foreground">@drawable/touch_fg</item>

    </style>

    <!--对话框按钮颜色-->
    <style name="appalertdialogbuttonstyle">
        <item name="android:textColor">@color/theme_color</item>

        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
        <item name="android:gravity">center_vertical|center_horizontal</item>
    </style>


    <style name="mydialog" parent="android:Theme.Material.Light.Dialog.Alert">
        <item name="buttonStyle">@style/appalertdialogbuttonstyle</item>
        <item name="android:foreground">@null</item>
    </style>
</resources>
